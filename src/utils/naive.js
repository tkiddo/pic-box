import {
  create,
  NButton,
  NForm,
  NFormItem,
  NSpace,
  NLayout,
  NInput,
  NLayoutHeader,
  NPopselect,
  NLayoutContent,
  NMessageProvider
} from 'naive-ui';

const naive = create({
  components: [
    NButton,
    NForm,
    NFormItem,
    NSpace,
    NLayout,
    NInput,
    NLayoutHeader,
    NPopselect,
    NLayoutContent,
    NMessageProvider
  ]
});

export default naive;
