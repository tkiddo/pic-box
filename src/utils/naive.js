import {
  create,
  NButton,
  NForm,
  NFormItem,
  NSpace,
  NLayout,
  NInput,
  NLayoutHeader
} from 'naive-ui';

const naive = create({
  components: [NButton, NForm, NFormItem, NSpace, NLayout, NInput, NLayoutHeader]
});

export default naive;
